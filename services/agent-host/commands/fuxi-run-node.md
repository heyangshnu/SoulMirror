---
description: 让伏羲执行单个节点或专题工单。
agent: fuxi
---

请作为伏羲 Agent 执行以下 FUXI-TASK：

$ARGUMENTS

要求：

1. 读取 `docs/FUXI_NODE_SCHEDULE.md`。
2. 确认对应 `fuxi-nodes/` 节点提示词。
3. 保留输入中的现实原文，不用推测替代原话。
4. 写入或生成写入 `01_命/` 的报告草案。
5. 明确说明是否触发 B09；除非任务明确要求 B09，否则不得触发。
6. 完成后输出 `AGENT_TASK_COMPLETE`。
